# 复制为 .env.development 后按需修改（不设则走下方默认行为）
#
# H5 开发（npm run dev:h5）：默认走 Vite 代理 /api → 127.0.0.1:5000，无需配置本项。
# 若显式设置 VITE_API_BASE_URL，将直连该地址（可能触发浏览器 CORS，需 pms-web 已配置 /api/mp CORS）。
#
# 其他端或需直连后端时，例如 http://192.168.1.100:5000
# VITE_API_BASE_URL=http://192.168.1.100:5000
#
# H5 用手机访问电脑上的后端时：填写开发机局域网 IP，不要用 localhost。
